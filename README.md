# Задание

Функционал Постов и их реквизитов.

*Много ошибок наделала при мердже*


## ветка main

Создаем функционал Постов 
https://vk.com/dev/objects/post
https://github.com/netology-code/kt-homeworks/blob/master/05_objects/assets/post.pdf

Задачи:

1. Посты и сервис для хранения постов
2. Wall, сохранение постов на стену пользователя
3. Автотесты


## ветка hw_2_3

Применение Nullable, наследование (добавляем Attachments)

Задачи:

1. одно из полей поста делаем Nullable
2. добавляем attachments
3. *не делала* переделываем attachments на sealed-классы

Описание задачи 3

В Kotlin предыдущую задачу можно решить с помощью запечатанных (sealed) классов.
Вы можете определить фиксированную иерархию типов (количество типов вложений у вас фиксировано).
Тогда в выражениях типа when вам не придётся писать else.

Нужно почитать документацию и сделать Pull Request с реализацией на базе sealed-классов.

Подказка: Не забывайте, что у всех наследников есть общее поле type. Для sealed-классов возможна следующая реализация:
``
    sealed class Attachment(val type: String)
    
    data class VideoAttachment(val video: Video) : Attachment("video")
    
    fun main() {
        val attachment: Attachment = VideoAttachment("stuff")
        println(attachment.type)
    }


## ветка hw_3_1

Добавить комменты, и при создании комментов выкидивать исключение, если поста не нашли.
Плюс в тесты добавить проверку на исключения.

Задачи:

1. создание коммента с выбросом исключения
2. добавление в тесты проверки на исключения
3. *не делала* Жалобы

Описание задачи 3:

Внедрите функцию "пожаловаться на комментарий"
https://vk.com/dev/wall.reportComment
Что нужно сделать:
- Реализовать саму функцию (для простоты храните эти репорты в отдельном массиве).
- Подумать, в каких случаях и какие нужно выкидывать исключения (неверным может быть не только ID комментария, но и причина).
- писать автотесты.

